# @synergy/ui - user interfaces for synergy-app

Components are styled with TailwindCSS using the `@synergy/configs/tailwind` config.
They need to be provided wrapped in a `ThemeProvider` to be styled.
